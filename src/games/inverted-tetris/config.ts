import { GB } from "../../config/AppConfig";

export const RISING = {
  INITIAL_INTERVAL_MS: 4500,
  MIN_INTERVAL_MS: 1800,
  INTERVAL_STEP_MS: 150,
  MIN_GAPS: 2,
  MAX_GAPS: 4,
  GARBAGE_COLOR_INDEX: 8,
  LINES_PER_LEVEL: 6,
};

export const GARBAGE_COLOR = GB.DARK;
