<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- AdMob App ID: Google's official test App ID. Ads are disabled for
             this build (see src/systems/AdsManager.ts) since AdMob is
             temporarily unavailable and mediation is moving to AppLovin MAX.
             Left in place, not removed: the bundled Google Mobile Ads SDK
             AAR has its own startup-time provider that expects this
             meta-data to exist regardless of whether the app ever calls
             AdMob.initialize() — removing it risks a boot crash, not just an
             inert unused value. Safe to actually remove once the AdMob
             dependency itself is fully replaced by AppLovin. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- AD_ID intentionally not declared: ads are disabled for this build
         (see AdsManager.ts), so this app does not currently use the
         advertising ID. Re-add once AppLovin is wired in and actually
         serving ads. -->
</manifest>
